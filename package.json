{
  "private": true,
  "packageManager": "pnpm@7.21.0",
  "scripts": {
    "build:crt": "pnpm --filter='@lkyl/create-react-template' build",
    "build:pubsub": "pnpm --filter='@lkyl/pubsub' build",
    "lint": "eslint . --ext .ts,.js",
    "lint:fix": "eslint . --ext .ts,.js --fix"
  }
}
